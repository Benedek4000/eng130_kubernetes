apiVersion: v1
kind: Service

metadata:
  name: app-svc
  namespace: default

spec:
  ports:
  - nodePort: 30001
    port: 80
    targetPort: 80

  selector:
    app: app

  type: NodePort